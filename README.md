# sqlx playground

Documenting how to use this library

## Requirements

Docker + docker-compose + rust toolchain

## Usage

In a terminal:

```bash
./manage/start_fresh_db
```

In another terminal:

```bash
cargo run
```
